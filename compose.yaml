name: web-research-mcp

x-firecrawl-env: &firecrawl-env
  REDIS_URL: redis://redis:6379
  REDIS_RATE_LIMIT_URL: redis://redis:6379
  PLAYWRIGHT_MICROSERVICE_URL: http://playwright-service:3000/scrape
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres
  POSTGRES_DB: postgres
  POSTGRES_HOST: nuq-postgres
  POSTGRES_PORT: 5432
  USE_DB_AUTHENTICATION: "false"
  NUM_WORKERS_PER_QUEUE: "4"
  CRAWL_CONCURRENT_REQUESTS: "5"
  MAX_CONCURRENT_JOBS: "3"
  BROWSER_POOL_SIZE: "3"

services:
  gateway-data-init:
    image: ${WEB_RESEARCH_MCP_IMAGE:-ghcr.io/source-inc/web-research-mcp:0.1.6@sha256:d5d9a4616d6f16d198b1b10b3e0c475e04cf0cf70dbbd0f32e8315cf8c13cb9a}
    user: "0:0"
    entrypoint: ["/bin/sh", "-c"]
    command: ["chown -R 10001:10001 /var/lib/web-research-mcp"]
    volumes:
      - gateway-data:/var/lib/web-research-mcp
    networks:
      - research
    restart: "no"

  gateway:
    image: ${WEB_RESEARCH_MCP_IMAGE:-ghcr.io/source-inc/web-research-mcp:0.1.6@sha256:d5d9a4616d6f16d198b1b10b3e0c475e04cf0cf70dbbd0f32e8315cf8c13cb9a}
    environment:
      WEB_RESEARCH_MCP_HTTP_BIND_ADDR: 0.0.0.0
      WEB_RESEARCH_MCP_HTTP_PORT: "9213"
      WEB_RESEARCH_MCP_DATA_DIR: /var/lib/web-research-mcp
      WEB_RESEARCH_MCP_ENABLE_SEARCH: "true"
      WEB_RESEARCH_MCP_ENABLE_EXTRACT: "true"
      WEB_RESEARCH_MCP_ENABLE_CRAWL: "false"
      WEB_RESEARCH_MCP_ENABLE_BROWSER: "false"
      WEB_RESEARCH_MCP_EXPOSED_TOOLS: web_collect_evidence,web_find_in_fetch
      SEARXNG_ENDPOINT: http://searxng:8080
      FIRECRAWL_ENDPOINT: http://firecrawl-api:3002
    ports:
      - "127.0.0.1:${WEB_RESEARCH_MCP_PORT:-9213}:9213"
    volumes:
      - gateway-data:/var/lib/web-research-mcp
    depends_on:
      gateway-data-init:
        condition: service_completed_successfully
      searxng:
        condition: service_healthy
      firecrawl-api:
        condition: service_healthy
    networks:
      - research
    restart: unless-stopped

  searxng:
    image: searxng/searxng@sha256:11a9b34cdc0b1ec2b991470a2762ecb5a1a531898289fb51dcd015260450729e
    environment:
      SEARXNG_BASE_URL: http://searxng:8080/
    volumes:
      - ./deploy/searxng/settings.yml:/etc/searxng/settings.yml:ro
    networks:
      - research
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/healthz | grep -q '^OK$'"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s
    restart: unless-stopped

  firecrawl-api:
    image: ghcr.io/firecrawl/firecrawl@sha256:6f3d6269ac282c4684ec17f977e4a429ce95e9d54bf303b9858b579b4ce8cc63
    command: node dist/src/harness.js --start-docker
    environment:
      <<: *firecrawl-env
      HOST: 0.0.0.0
      PORT: "3002"
      INTERNAL_PORT: "3002"
      WORKER_PORT: "3005"
      EXTRACT_WORKER_PORT: "3004"
      NUQ_RABBITMQ_URL: amqp://rabbitmq:5672
      HARNESS_STARTUP_TIMEOUT_MS: "120000"
      ENV: local
    depends_on:
      redis:
        condition: service_healthy
      playwright-service:
        condition: service_healthy
      nuq-postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - research
    extra_hosts:
      - host.docker.internal:host-gateway
    cpus: 4.0
    mem_limit: 8g
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3002/').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 5s
      timeout: 5s
      retries: 24
      start_period: 30s
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    restart: unless-stopped

  playwright-service:
    image: ghcr.io/firecrawl/playwright-service@sha256:468009bae00911d40d7120d58489a1d529362c22c45585cb9076094fe61b0025
    environment:
      PORT: "3000"
      MAX_CONCURRENT_PAGES: "5"
    networks:
      - research
    cpus: 2.0
    mem_limit: 4g
    tmpfs:
      - /tmp/.cache:noexec,nosuid,size=1g
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/').then(r=>process.exit(r.status<500?0:1)).catch(()=>process.exit(1))"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s
    restart: unless-stopped

  redis:
    image: redis@sha256:ff02b58f971e7d7d156a1267e283fcbbeee91773b6aa36c49dac28ecfe28eadf
    command: redis-server --bind 0.0.0.0
    networks:
      - research
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 12
    restart: unless-stopped

  rabbitmq:
    image: rabbitmq@sha256:e582c0bc7766f3342496d8485efb5a1df782b5ce3886ad017e2eaae442311f69
    command: rabbitmq-server
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "check_running"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 20s
    networks:
      - research
    restart: unless-stopped

  nuq-postgres:
    image: ghcr.io/firecrawl/nuq-postgres@sha256:aed86f62858f29bd971abddcdeb301c12888098d2cf5d33c1ba42b053bc460f6
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    networks:
      - research
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d postgres"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s
    restart: unless-stopped

networks:
  research:
    driver: bridge

volumes:
  gateway-data:
